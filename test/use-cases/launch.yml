User launches the skill:
  - beforeEach: 'launch'
  - afterEach: 'clear'
  - LaunchIntent:
      alexaResponse: Launch.FirstTimeUser
  - AMAZON.StopIntent:
      alexaResponse: Exit
      shouldEndSession: true
