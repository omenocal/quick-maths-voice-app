{
  "name": "quick-maths",
  "version": "0.0.1",
  "main": "src/app/index.js",
  "scripts": {
    "deploy": "gulp deploy; gulp upload",
    "test": "NODE_ENV=local.example mocha test/*.spec.* test/**/*.spec.*",
    "test-ci": "NODE_ENV=local.example nyc mocha --colors --reporter mocha-jenkins-reporter test test/*.spec.* test/**/*.spec.*",
    "watch": "nodemon --inspect server.js",
    "lint": "eslint src/ test/ server.js",
    "lint-fix": "eslint --fix src/ test/ server.js",
    "interaction": "voxa interaction",
    "report": "nyc report --reporter=json && nyc report --reporter html && nyc report --reporter=lcov && nyc report --reporter=cobertura"
  },
  "author": "Octavio Menocal",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.502.0",
    "lambda-log": "^2.3.0",
    "lodash": "^4.17.15",
    "moment-timezone": "^0.5.26",
    "number-to-words": "^1.2.4",
    "prettier": "^1.18.2",
    "reading-time": "^1.2.0",
    "voxa": "^3.3.0",
    "voxa-dashbot": "2.0.3",
    "voxa-ga": "^2.0.1"
  },
  "devDependencies": {
    "alexa-mime": "^0.0.6",
    "ask-cli": "^1.7.9",
    "aws-sdk-mock": "^4.5.0",
    "chai": "^4.2.0",
    "coveralls": "^3.0.5",
    "del": "^5.0.0",
    "eslint": "^6.1.0",
    "eslint-config-airbnb-base": "^13.2.0",
    "eslint-plugin-import": "^2.18.2",
    "express": "^4.17.1",
    "gulp": "^3.9.1",
    "gulp-install": "^1.1.0",
    "gulp-zip": "^5.0.0",
    "merge-stream": "^2.0.0",
    "mocha-jenkins-reporter": "^0.4.1",
    "mocha-junit-reporter": "^1.23.1",
    "nock": "^10.0.6",
    "node-aws-lambda": "^0.1.8",
    "nodemon": "^1.19.1",
    "nyc": "^14.1.1",
    "run-sequence": "^2.2.1",
    "simple-mock": "^0.8.0",
    "voxa-cli": "2.2.0"
  },
  "directories": {
    "test": "test"
  }
}
